database:
  kind: postgres
  host: db
  port: 5432
  user: ${POSTGRES_USER:-dipdup}
  password: ${RDS_PASSWORD:-flipco00}
  database: ${POSTGRES_DB:-dipdup}
  schema_name: hic_et_nunc

hasura:
  url: http://hasura:8080
  admin_secret: ${ADMIN_SECRET:-roosterSteelyOkinawa}
